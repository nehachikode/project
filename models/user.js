module.exports = {
    getUser: async(con, data) => {
        let query = ``;
        return await con.query(query);
    },
    addUser: async (con, data) => {
        let query = ``;
        return await con.query(query);
    }
};